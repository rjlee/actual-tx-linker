name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  ci:
    uses: rjlee/actual-auto-ci/.github/workflows/ci.yml@v1
    with:
      node-version: '22'
      package-manager: npm
      lint: true
      format-check: true
      test: true
