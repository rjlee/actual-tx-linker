name: CI & Release

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: write
  issues: write
  packages: write
  attestations: write
  id-token: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install
        working-directory: ./actual-tx-linker

      - name: Lint code
        run: npm run lint
        working-directory: ./actual-tx-linker

      - name: Check formatting
        run: npm run format:check
        working-directory: ./actual-tx-linker

      - name: Run tests
        run: npm test
        working-directory: ./actual-tx-linker

      - name: Run semantic-release
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx semantic-release
        working-directory: ./actual-tx-linker

